{
  "name": "ilocarpay-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Suíte de testes das Firestore Security Rules (emulador local, sem contato com o projeto real)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-ilocarpay-rules --config ../../firebase.json \"node firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
